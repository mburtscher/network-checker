#!/usr/bin/env php
<?php

require __DIR__ . "/../vendor/autoload.php";

use NetworkTester\Commands\RunCommand;
use Symfony\Component\Console\Application;

$app = new Application("Network Tester");
$app->add(new RunCommand());
$app->run();
